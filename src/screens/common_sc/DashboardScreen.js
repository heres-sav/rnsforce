import React, {
    Node,
    useContext,
    useEffect,
    useState
} from 'react';
import {
    View,
    SafeAreaView,
    Text,
    FlatList,
    StyleSheet,
    TouchableOpacity
} from 'react-native';
import Header from '../../components/header';

const DashboardScreen = () => {
    return (<View>
        <Header />
    </View>);
}

export default DashboardScreen